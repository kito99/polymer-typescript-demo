namespace App {

    const {customElement, property, query, queryAll, observe, computed} = Polymer.decorators;

    @customElement('my-view1')
    export class MyView1 extends Polymer.Element {
        
    }

}